import { ReactNode } from 'react';
import { EquationParserError } from 'equation-parser';
import { EquationResolveError } from 'equation-resolver';
declare type Combined = EquationParserError | EquationResolveError;
export declare type ErrorHandler = {
    [Key in Combined['errorType']]?: (node: Extract<Combined, {
        errorType: Key;
    }>) => ReactNode;
};
export declare const defaultErrorHandler: ErrorHandler;
export {};
