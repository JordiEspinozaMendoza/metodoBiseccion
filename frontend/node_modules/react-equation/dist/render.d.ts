import React from 'react';
import { EquationNode, EquationParserError } from 'equation-parser';
import { EquationResolveError } from 'equation-resolver';
import { Rendering } from './Rendering';
import { RenderingPart } from './RenderingPart';
import { RenderOptions } from './RenderOptions';
export declare const render: (node: EquationNode | EquationParserError | EquationResolveError, { errorHandler, className, style }?: RenderOptions) => JSX.Element;
export declare function renderInternal(tree: EquationNode, skipParentheses?: boolean, ...initial: RenderingPart[]): Rendering;
export declare function toRendering(parts: RenderingPart[]): Rendering;
export declare function pushTree(node: EquationNode, current: RenderingPart[]): RenderingPart[];
export declare function simplePart(value: string, style?: React.CSSProperties): {
    type: string;
    props: {
        style: React.CSSProperties | undefined;
    };
    children: string;
    aboveMiddle: number;
    belowMiddle: number;
};
